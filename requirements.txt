# Core AI providers
anthropic>=0.40.0
openai>=1.50.0        # also covers OpenRouter (same API format, different base URL)

# HTTP (Ollama streaming, future web tools)
aiohttp>=3.9.0

# Environment / config
python-dotenv>=1.0.0
pyyaml>=6.0.1

# Terminal UX
rich>=13.7.0

# Tier 3 additions (install when you reach voice)
# deepgram-sdk>=3.7.0
# elevenlabs>=1.5.0
# pyaudio>=0.2.14       # or sounddevice>=0.4.7 on some systems
