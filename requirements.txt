# Core AI providers
anthropic>=0.40.0
openai>=1.50.0        # also covers OpenRouter (same API format, different base URL)

# HTTP (Ollama streaming, future web tools)
aiohttp>=3.9.0

# Read-only Supabase/Postgres tools (query_analytics, etc.)
asyncpg>=0.29.0

# Environment / config
python-dotenv>=1.0.0
pyyaml>=6.0.1

# Terminal UX
rich>=13.7.0

# Voice V1: Deepgram STT is called directly over aiohttp (no SDK needed).
# Piper TTS runs the model locally — no API key, no per-character cost.
piper-tts>=1.4.0
